apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory-api-chart.fullname" . }}
  labels:
    {{- include "memory-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memory-api-chart.selectorLabels" . | nindent 4 }}
